/* Ecrire un fonction constructeur des trois objets Product, Category et Client. */
function Product(nom, description, prix){
  Person.nextId = (Person.nextId || 0) + 1;
  this.id = Person.nextId;
  /* Completer */
}

/* Les autres fonctions constructeurs */